## Razorpay Payment Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      RAZORPAY PAYMENT INTEGRATION                       │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│     USER     │
│   (Browser)  │
└──────┬───────┘
       │
       │ 1. Navigates to /payment/:applicationId
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│                        PAYMENT PAGE                              │
│  • Fetches application details from backend                     │
│  • Displays visa fee (from VisaType model)                      │
│  • Shows eSIM addon options                                     │
│  • Calculates total: Visa Fee + eSIM (if selected)             │
└──────┬───────────────────────────────────────────────────────────┘
       │
       │ 2. User clicks "Pay ₹X" button
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│            FRONTEND: handleRazorpayPayment()                     │
│  • Loads Razorpay SDK script                                    │
│  • Calls: POST /api/payment/create-order                        │
│    - applicationId                                              │
│    - esimPrice (if selected)                                    │
└──────┬───────────────────────────────────────────────────────────┘
       │
       │ 3. Create Order Request
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│         BACKEND: POST /api/payment/create-order                  │
│  • Validates user authentication                                │
│  • Fetches Application + VisaType from DB                       │
│  • Calculates total: visaFee + esimPrice                        │
│  • Calls Razorpay: razorpay.orders.create()                     │
│  • Stores order ID in Application.payment                       │
│  • Returns: order object + key_id                               │
└──────┬───────────────────────────────────────────────────────────┘
       │
       │ 4. Order Created Response
       │    { order_id, amount, currency, key_id }
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│              RAZORPAY CHECKOUT MODAL OPENS                       │
│  ┌────────────────────────────────────────────────────┐         │
│  │  🔒 Secure Payment by Razorpay                    │         │
│  │                                                    │         │
│  │  Amount: ₹X                                       │         │
│  │  Application: IV2026XXXXXX                        │         │
│  │                                                    │         │
│  │  Select Payment Method:                           │         │
│  │  [💳 Card] [📱 UPI] [🏦 Net Banking] [💰 Wallet]│         │
│  │                                                    │         │
│  │  Enter payment details...                         │         │
│  │                                                    │         │
│  │  [        Pay ₹X Securely        ]               │         │
│  └────────────────────────────────────────────────────┘         │
└──────┬───────────────────────────────────────────────────────────┘
       │
       │ 5. User enters payment details & submits
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│                  RAZORPAY PROCESSES PAYMENT                      │
│  • Validates payment details                                    │
│  • Charges the amount                                           │
│  • Generates payment_id & signature                             │
│  • Returns to callback: handler()                               │
└──────┬───────────────────────────────────────────────────────────┘
       │
       │ 6. Payment Success Callback
       │    { razorpay_order_id, razorpay_payment_id, 
       │      razorpay_signature }
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│              FRONTEND: Payment Handler                           │
│  • Receives payment response                                    │
│  • Calls: POST /api/payment/verify                              │
│    - razorpay_order_id                                          │
│    - razorpay_payment_id                                        │
│    - razorpay_signature                                         │
│    - applicationId                                              │
│    - esim data (if selected)                                    │
└──────┬───────────────────────────────────────────────────────────┘
       │
       │ 7. Verify Payment Request
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│          BACKEND: POST /api/payment/verify                       │
│  • Verifies signature using HMAC SHA256:                        │
│    signature = HMAC(order_id|payment_id, secret)                │
│  • Fetches payment details from Razorpay API                    │
│  • Updates Application:                                         │
│    - payment.razorpayOrderId                                    │
│    - payment.razorpayPaymentId                                  │
│    - payment.razorpaySignature                                  │
│    - payment.amount, currency, method                           │
│    - paymentStatus = 'completed'                                │
│    - status = 'under-review'                                    │
│    - esim data (if selected)                                    │
│  • Sends payment confirmation email                             │
│  • Returns success response                                     │
└──────┬───────────────────────────────────────────────────────────┘
       │
       │ 8. Verification Success Response
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│                    FRONTEND: Success Handler                     │
│  • Shows success toast message                                  │
│  • Redirects to /profile after 2 seconds                        │
└──────┬───────────────────────────────────────────────────────────┘
       │
       │ 9. Redirect to Profile
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│                       PROFILE PAGE                               │
│  • User sees updated application status                         │
│  • Application shows: "Payment Completed"                       │
│  • Status: "Under Review"                                       │
│  • Payment receipt available                                    │
└──────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════
                         ERROR SCENARIOS
═══════════════════════════════════════════════════════════════════

❌ User Cancels Payment:
   Razorpay Modal → ondismiss() → Show warning toast → Stay on page

❌ Payment Fails:
   Razorpay → Error callback → Show error toast → Allow retry

❌ Signature Verification Fails:
   Backend → Return 400 error → Show error → Contact support message

❌ Network Issues:
   Any step → Catch error → Show friendly message → Allow retry


═══════════════════════════════════════════════════════════════════
                        DATABASE UPDATES
═══════════════════════════════════════════════════════════════════

Application Document Fields Updated:
┌─────────────────────────────────────────────────────────────────┐
│ payment: {                                                      │
│   razorpay_order_id: "order_xxxxxxxxxxxxx"                    │
│   razorpay_payment_id: "pay_xxxxxxxxxxxxx"                    │
│   razorpay_signature: "abc123..."                             │
│   amount: 5000                                                 │
│   currency: "INR"                                              │
│   method: "upi" | "card" | "netbanking" | "wallet"           │
│   status: "captured"                                           │
│   paidAt: Date                                                 │
│ }                                                               │
│ paymentStatus: "completed"                                      │
│ status: "under-review"                                          │
│ esim: { ... } (if eSIM was selected)                           │
└─────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════
                    SECURITY MECHANISMS
═══════════════════════════════════════════════════════════════════

1. ✅ Signature Verification
   HMAC-SHA256(order_id|payment_id, secret_key)
   
2. ✅ Server-Side Amount Validation
   Amount calculated on backend, not trusted from frontend
   
3. ✅ Authentication Check
   Firebase Auth token required for all API calls
   
4. ✅ Ownership Verification
   User can only pay for their own applications
   
5. ✅ SSL/TLS Encryption
   All data encrypted in transit (HTTPS)
   
6. ✅ PCI DSS Compliance
   Card details never touch your server


═══════════════════════════════════════════════════════════════════
                    WEBHOOK (OPTIONAL)
═══════════════════════════════════════════════════════════════════

Razorpay → POST /api/payment/webhook
│
├─ Event: payment.captured
│  └─ Log payment success
│
├─ Event: payment.failed  
│  └─ Log payment failure
│
└─ Event: order.paid
   └─ Additional confirmation

Note: Webhooks provide backup notification if frontend callback fails
```
